# CLAUDE.md — Claude Code 運用ルール

Izumishin さんの Claude Code 利用における共通ルール。
（2026-07-12 の仕組み棚卸しレポートに基づき作成）

## このリポジトリについて

- タスク管理アプリ（Express + JSONファイル保存）のコード置き場。
- **アプリは廃止決定済み**（未使用・追加ボタン故障のため）。公開デプロイはユーザーがホスティング側で停止・削除する。
- 今後このリポジトリは、運用ルール（本ファイル）と棚卸し関連の置き場として使う。

## セキュリティルール（最優先）

1. APIキー・パスワード・トークンは**コードに直書きしない**。
   - Node なら環境変数、GAS ならスクリプトプロパティに保管する。
2. `.env` や認証情報ファイルは **git にコミットしない**（`.gitignore` に追加してから作る）。
3. レポート・チャット出力に**キーやパスワードの値そのものを含めない**。
4. セキュリティ指摘は**推測で書かず、コマンドで確認した事実だけ**を報告する。
   - 「危険（実害を確認）」と「要確認（疑いあり）」を必ず区別する。
5. Webに公開するアプリには必ず認証を付ける。認証なしの公開は禁止。

## コネクタ（Gmail / Google Drive / Google Calendar）の運用

- 原則「**使う時だけ接続、終わったら切断**」。常時接続にしない。
- Drive には会計データ等の機微情報があるため、Claude に依頼する際は
  対象ファイル・フォルダを明示して、探索範囲を絞る。
- Claude 側からコネクタの接続/切断はできないため、切り替えはユーザーが
  claude.ai の設定画面で行う。

## 修正作業の進め方

1. Claude はいきなり直さず、**先に修正方針を提案**する。
2. ユーザーの**承認を得てから**修正に着手する。
3. 削除・上書き・公開などの取り返しのつきにくい操作は、都度確認する。

## 日課（繰り返し作業）

- 朝の ToDo 整理（morning-todo スキル）
- 毎日の健康記録（health-advisor スキル）※記録先は今後1か所に統一する
- 会計ツール（GAS）での記帳

## 既知の宿題（棚卸しレポートより）

- [ ] タスク管理アプリの公開デプロイを停止・削除する（ユーザー作業）
- [ ] 会計ツール（GAS）のコード点検（キー直書きの有無）— コード貼り付け方式で実施予定
- [ ] health-advisor の記録先を1か所に決める
- [ ] morning-todo を自動化するか手動運用にするか決める
