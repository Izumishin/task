# CLAUDE.md

このリポジトリには2つの独立したものが含まれます。

1. ルート直下：シンプルなタスク管理アプリ（`server.js` / `public/`）。総目次作業とは無関係。
2. `toc_tool/`：**月刊誌PDF → 総目次Excel 自動転記ツール**（メインの作業対象）。

---

## 総目次作業を頼まれたときの手順（toc_tool）

ユーザーは毎月、**当月号のPDF**（分割数は任意）と**最新の総目次Excel**を添付して
「◯月号を追加して」と依頼してくる。前年の同月データは**置き換え**る。対象は
**月刊号のみ**（増刊号は対象外＝来たら個別に相談）。

転記する列：**B=区分／C=論文表題／D=著者／E=頁／G=掲載月／I=読み**。
A=共著順は転記しない。F/H の括弧と J/K の姓名分割数式はツールが自動付与する。

### 実行ステップ

```
cd toc_tool
pip install -r requirements.txt        # 初回のみ（pymupdf, openpyxl）
```

1. **目次を読む**
   `python add_month.py dump --pdf <当月PDF...>` で誌面ページ対応と目次全文を表示。
   目次ページ（誌面p.1）から、記事の並び・区分・表題（副題の `――`・改行含む）・
   開始頁・筆頭著者を把握する。**文献抄録・書評は目次に表題が無い**ので、その記事の
   本文ページを `python add_month.py page --num <頁>` で開いて表題を取る。

2. **共著者を確定**
   共著記事は本文先頭ページに漢字著者が全員載っている。`page` サブコマンドが
   漢字著者・ローマ字・頁範囲を抽出して見せるので、**誌面の表示順**で全員を拾う
   （抽出がタイトルと癒着することがあるため、表示テキストで必ず目視確認する）。

3. **articles JSON を書く**（`sample_articles.json` がテンプレート）
   1記事1オブジェクト、`authors` は表示順に全員。著者なしの特集テーマ行は
   `"theme": true`。読みを自分で確定したら `"readings": {"著者名":"よみ"}` で固定。
   - **特集の総合テーマ行の扱いは号ごとに違う**。その月の目次の見た目に合わせる
     （テーマを先頭記事の表題に兼ねる号もあれば、著者なし独立行の号もある）。

4. **書き込む**
   ```
   python add_month.py write --excel <Excel> --rows articles.json \
       --out <出力Excel> --month <N> --pdf <当月PDF...>
   ```
   読み（I列）は「既存Excel辞書 → ローマ字変換（黄色で要確認）」の順で自動解決。
   実行後に**要確認リスト（黄色セル）**が出る。

5. **要確認セルを潰す**
   黄色になった新規著者・外国人名・不明区分は、**必ずPDFの該当ページを実際に読んで**
   正しい読み（`せい　めい`／全角スペース区切り）を確定し、`readings` に入れて
   再実行するか、Excelのセルを直接直す（直したら黄色も外す）。

6. **納品**
   更新済みExcelを `SendUserFile` で返し、要確認だった点・置き換えた月・追加行数を
   日本語で簡潔に報告する。

### 検証（毎回やる）

- 出力xlsxを openpyxl で再読込し、破損なし・J/K数式が各行の行番号を正しく参照して
  いることを確認（`_renumber_split_formulas` が全行を再採番する）。
- 対象月**以外**のブロックが変わっていないこと（行の挿入/削除で下方向がずれても
  数式は再採番されるが、値は不変のはず）。

### 実装メモ

- 確実性の高い機械処理は `toc_lib.py` に集約：`build_page_map`（誌面頁↔PDF頁）、
  `build_reading_dict`（漢字→よみ、多数決）、`write_month`（月ブロック置換＋書式/
  括弧/数式再現）、`resolve_reading`（辞書→ローマ字）。`romaji.py` はローマ字→
  ひらがな（長音は要確認扱い）。
- 目次の全自動パース（`parse_toc`）は号ごとの書式揺れに弱いので**依存しない**。
  目次の読み取りはClaude自身が行い、ツールは本文抽出・読み・Excel書き込みを担う。
- 1月号での回帰テスト実績：手作業版の全30行と一致（唯一の差分は元データの
  手入力ミス「福家＝ふくやま」をツールが多数決で「ふくや」に自動修正したもの）。
