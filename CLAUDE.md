# CLAUDE.md

Claude Code が毎回読み込む常設ルール。

## トークン運用ルール

目的はトークン使用量の最適化。ただし節約を目的化させず、いちばんパフォーマンスが高い形を選ぶ。
節約と品質がぶつかったら品質を取り、その判断を一言添える。

### 1. ファイルは全部読まない

- まず `Grep` / `Glob` で場所を特定し、`Read` は必要な範囲だけにする（`offset` / `limit` を使う）。
- 全体を通読するのは、構造の把握が本当に必要なときだけ。その場合は理由を一言添える。
- 生成物・ロックファイル・ビルド成果物・`node_modules` は読まない。
- 一度読んだ内容は読み直さない。すでに分かっていることを再確認するために読むのは無駄。

### 2. 広い調べ物はサブエージェントに任せ、結論だけ受け取る

- 複数ファイル・複数ディレクトリを横断する調査、仕様の洗い出し、影響範囲の特定は `Agent`（Explore / general-purpose）に委譲する。
- 依頼文には「知りたい結論」を書く。資料を集めてこさせるのではなく、答えを返させる。
- 調査で読んだファイルの中身を、そのままメインの会話に持ち込まない。残すのは要点とファイルパス・行番号だけ。
- 外部ページの取得も同じ。ページ本文ではなく、要約・結論を受け取る。

### 3. 小さな作業はサブエージェントに任せない

- 直す場所が分かっている修正、1〜2ファイルの編集、既知のコマンド実行は自分でやる。
- 委譲するとコンテキストを引き継げず、起動コストもかかる。判断基準は「探す必要があるか」。探す必要がなければ自分でやる。

### 4. 場所があいまいなら、探し回る前に聞く

- 対象ファイル・設定の置き場所・参照すべき資料が特定できないときは、総当たり検索を始める前に確認する。
- 目安は、見当をつけた検索を2〜3回試して当たらなければ、それ以上広げずに質問する。
- 前提が曖昧なまま決め打ちで進めない。解釈が複数あるときは候補を示して確認する。

### 5. 同じやり方で2回失敗したら、方法を変える

- 同一のコマンド・同一のアプローチを3回目は実行しない。
- いったん止めて「何が起きたか / 想定と何が違ったか / 次に試す別の手」を整理してから進む。
- 「もう一度やってみる」だけの再試行は禁止。原因が分からなければ、状況を報告して聞く。

### 6. 削ってはいけないこと（節約の対象外）

以下は「トークンがかかるから」を理由に省略しない。

- **作業前の記録確認**: `git status` / `git log` / 直前の変更内容、関連する既存実装の確認。
- **送信・公開・外部反映の前の内容チェック**: コミット内容（`git diff`）、PR や Issue の本文、メールやメッセージの文面、外部サービスへ送るデータ。一度出したものは取り消せない前提で確認する。
- **破壊的操作の前の対象確認**: 削除・上書き・リセットの前に、その対象が本当に合っているかを見る。
- **完了報告の正確さ**: テストが落ちたこと、スキップした作業、未検証の箇所はそのまま伝える。曖昧にしない。

## 話し方

- わかりやすく端的に。くだけすぎず、親しみやすく。
- 専門用語は必要なときだけ使い、使う場合は意味も補足する。
